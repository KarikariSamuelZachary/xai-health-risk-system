# xai-health-risk-system

An end-to-end explainable machine learning system that predicts an individual’s health risk as a calibrated probability score from structured clinical and lifestyle data, while providing transparent, human-interpretable explanations for each prediction.

## Features
- Predicts health risk as a probability (0-1)
- Provides feature-level explanations for each prediction
- Handles class imbalance
- Designed for deployment as a web-based API

## Setup (Local)
1. Clone the repository:
   ```bash
   git clone <repo-url>
   cd <repo-name>

2. Install dependencies:
    ```bash
    pip install -r requirements.txt
